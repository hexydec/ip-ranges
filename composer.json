{
	"name": "hexydec/ip-ranges",
	"type": "project",
	"autoload": {
		"classmap": [
			"src/"
		]
	},
    "require-dev": {
        "phpunit/phpunit": "^13.2",
        "phpstan/phpstan": "^2.2"
    },
    "scripts": {
        "test": "@php -d xdebug.mode=off vendor/bin/phpunit",
        "analyse": "@php -d xdebug.mode=off vendor/bin/phpstan analyse --memory-limit=512M"
    }
}
